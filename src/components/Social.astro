---
const { platform, username } = Astro.props;
---

<a
  href={`https://www.${platform.toLowerCase()}.com/${username}`}
  class="hover:bg-[#f6c445] focus:bg-[#f6c445]"
  target="_blank"
  rel="noopener noreferrer">{platform}</a
>
